const Backbone = require('Backbone');
const ProjectRow = require('./OrganizationRow');

class ProjectGrid extends Backbone.View {
	constructor(options) {
		super(options);
	}
}

module.exports = ProjectGrid;